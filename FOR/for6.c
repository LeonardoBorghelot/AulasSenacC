#include <stdio.h>

    int main() {

        int n1, r, par, impar, pos, neg;

            for(n1 = 0; n1 <5; n1 ++){    
                printf("Digite um valor? ");
                    scanf("%d", &r);                
            }

            if(r 5 % 2 == 0)
                printf("Pares: ");
            else
                printf("Impares: ");
            










    }