#include <stdio.h>

    int main(){

            int n1;
            
                printf("Digite um numero: ");
                    scanf("%d", &n1);

                if(n1 > 1)
                    printf("Seu valor eh POSSITIVO!");
                
                if(n1 <= 0)
                    printf("Seu valor eh NEGATIVO!");
    }